function greeting() {
  let userName = "Santiago"; //defir variable solo dentro de la fuction
  console.log(userName);

  if (userName === "Santiago") {
    //Validacion
    console.log(`Hello ${userName}!`);
  }
}

greeting();
console.log(userName);
